{
  "name": "voting-application",
  "description": "backend voting application to be used by frontend development team.",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "@koa/cors": "^3.3.0",
    "@koa/router": "^10.1.1",
    "aws-sdk": "^2.799.0",
    "https": "^1.0.0",
    "koa": "^2.13.4",
    "koa-body": "^4.2.0",
    "serverless-http": "^3.0.1"
  },
  "scripts": {
    "start": "sam local start-api --profile dev --env-vars env.json --docker-network local-api-network -p 3100 --force-image-build --skip-pull-image"
  }
}
